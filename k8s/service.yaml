apiVersion: v1
kind: Service
metadata:
  name: fracture-classifier
spec:
  type: NodePort
  selector:
    app: fracture-classifier
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080